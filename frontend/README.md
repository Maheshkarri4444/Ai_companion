# Frontend (Next.js)

The learner workspace and admin console. See the [root README](../README.md) for setup and [docs/ARCHITECTURE.md](../docs/ARCHITECTURE.md) §28 for the frontend architecture.

```bash
npm run dev
```

Requires `BACKEND_URL` in `.env.local` (see `.env.example`); `/api/*` is proxied there.
